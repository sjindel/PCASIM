#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include "dSFMT.h"
#include "sim.h"

// Current context is just a pcea1d (for now).
typedef peca1d context;

// sim_table is the hashtable that will store the simulations.
typedef struct sim_table
{

} sim_table;
